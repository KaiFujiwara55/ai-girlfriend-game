[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start:linebot"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "always"

[variables]
NODE_ENV = "production"
PORT = "${{ PORT }}"